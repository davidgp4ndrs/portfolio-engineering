---
import Layout from "../../layouts/Layout.astro";
import IndexTemplate from "../../components/IndexTemplate.astro";
import { indexContent } from "../../data/indexContent";

const config = {
  titleIcon: "fas fa-terminal",
  quoteIcon: "fas fa-quote-left",
};
---

<Layout title="Portfolio">
  <IndexTemplate content={indexContent.es} {...config} />
</Layout>
